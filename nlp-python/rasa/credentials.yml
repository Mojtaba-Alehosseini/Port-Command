# Credentials — Port Command Genova (task 12).
# Intentionally empty: no messaging channels. The Java bridge (task 14) calls the
# HTTP API (POST /model/parse) enabled by `rasa run --enable-api`, which needs no
# channel credentials.
